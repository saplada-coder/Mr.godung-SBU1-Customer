# คัดลอกเป็น .env.local แล้วเติมค่าจริง — หรือดึงอัตโนมัติด้วย: vercel env pull .env.local
# (ไฟล์ .env.local จริงไม่ขึ้น git)

# --- ฐานข้อมูล Neon (provision ผ่าน Vercel Marketplace) ---
DATABASE_URL=

# --- Clerk (ล็อกอิน Google) ---
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
